---
phase: 03-core-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/components/navigation/Header.svelte, src/lib/components/navigation/Sidebar.svelte, src/lib/components/navigation/index.ts, src/routes/+layout.svelte]
autonomous: true
---

<objective>
Create responsive navigation shell with header and sidebar components.

Purpose: Establish the core layout structure that will contain module navigation and content.
Output: Header component with app branding and mobile menu, Sidebar component for module list, updated root layout integrating both.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-05-SUMMARY.md

# Existing components to use:
@src/lib/components/ui/index.ts
@src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header navigation component</name>
  <files>src/lib/components/navigation/Header.svelte</files>
  <action>
Create responsive header component with:
- App logo/title "Data Analytics in Marketing" on left
- Desktop: horizontal module navigation links (placeholder, will connect to data in 03-02)
- Mobile: hamburger menu button that toggles sidebar visibility
- Use Svelte 5 $props() and $state() patterns
- Import IconButton from $lib/components/ui for hamburger
- Dark mode support with dark: classes
- Sticky positioning at top

Props:
- onMenuToggle: () => void (for mobile menu)
- isMenuOpen: boolean

Use Tailwind for styling. Keep header height consistent (h-16).
  </action>
  <verify>Component renders without errors, responsive breakpoints work</verify>
  <done>Header component with responsive behavior and dark mode support</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar navigation component</name>
  <files>src/lib/components/navigation/Sidebar.svelte</files>
  <action>
Create sidebar component for module navigation:
- Fixed position on desktop (left side, below header)
- Slide-in overlay on mobile (controlled by isOpen prop)
- Module list will be passed as prop (actual data comes from 03-02)
- Active module highlighting based on current route
- Progress indicators per module (placeholder, will connect to data later)
- Smooth transitions for mobile open/close
- Dark mode support

Props:
- modules: Array<{ id: string; name: string; lectures: number }>
- isOpen: boolean (for mobile)
- onClose: () => void (for mobile overlay dismiss)

Use Svelte 5 patterns. Import Button from $lib/components/ui.
Desktop width: w-64. Mobile: full overlay with backdrop.
  </action>
  <verify>Component renders, mobile slide-in animation works</verify>
  <done>Sidebar with module list, responsive behavior, and route-aware active state</done>
</task>

<task type="auto">
  <name>Task 3: Create navigation barrel export and update root layout</name>
  <files>src/lib/components/navigation/index.ts, src/routes/+layout.svelte</files>
  <action>
**index.ts:**
```typescript
export { default as Header } from './Header.svelte';
export { default as Sidebar } from './Sidebar.svelte';
```

**+layout.svelte updates:**
- Import Header and Sidebar from $lib/components/navigation
- Add $state for mobile menu open/close
- Placeholder module data (will be replaced by real data in 03-02):
  ```typescript
  const modules = [
    { id: '1', name: 'Causal Analysis', lectures: 8 },
    { id: '2', name: 'Module 2', lectures: 0 },
    // ... placeholder for 8 modules
  ];
  ```
- Layout structure:
  - Header (sticky top)
  - Sidebar (fixed left on desktop, overlay on mobile)
  - Main content area (with left margin on desktop to account for sidebar)
- Dark mode: Update bg-slate-50 to bg-white dark:bg-slate-900
- Remove the basic nav from existing layout, replace with Header component
  </action>
  <verify>npm run dev shows navigation shell, mobile menu toggles sidebar</verify>
  <done>Complete navigation shell with header, sidebar, and responsive layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Header displays on all pages
- [ ] Sidebar shows module list
- [ ] Mobile menu toggle works
- [ ] Dark mode works for navigation components
</verification>

<success_criteria>
- All tasks completed
- Navigation shell is responsive (desktop sidebar, mobile overlay)
- Components follow established patterns (Svelte 5, Tailwind, dark mode)
- Ready for module data integration in 03-02
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-navigation/03-01-SUMMARY.md`
</output>
