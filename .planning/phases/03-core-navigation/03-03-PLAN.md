---
phase: 03-core-navigation
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [src/routes/module/[slug]/+page.svelte, src/routes/module/[slug]/+page.ts, src/routes/+page.svelte, src/routes/+layout.svelte]
autonomous: false
---

<objective>
Create module routes and landing pages with navigation integration.

Purpose: Complete the navigation system by adding module detail pages and connecting navigation to real data.
Output: Working /module/[slug] routes, module landing page template, navigation integrated with module data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan outputs needed:
@.planning/phases/03-core-navigation/03-01-SUMMARY.md
@.planning/phases/03-core-navigation/03-02-SUMMARY.md

# Source files:
@src/lib/data/modules.ts
@src/lib/types/module.ts
@src/lib/components/navigation/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create module route with data loading</name>
  <files>src/routes/module/[slug]/+page.ts, src/routes/module/[slug]/+page.svelte</files>
  <action>
**+page.ts (load function):**
```typescript
import { error } from '@sveltejs/kit';
import { getModuleBySlug } from '$lib/data/modules';
import type { PageLoad } from './$types';

export const load: PageLoad = ({ params }) => {
  const module = getModuleBySlug(params.slug);

  if (!module) {
    throw error(404, 'Module not found');
  }

  return { module };
};
```

**+page.svelte:**
Create module landing page with:
- Module header (name, description, icon)
- Lecture list using Card components from design system
- Each lecture card shows: title, description, status badge, visualization/animation indicators
- Coming soon badge for draft/coming-soon lectures
- Progress indicator (placeholder - 0% complete)
- Dark mode support

Import from $lib/components/ui:
- Container, Grid, Card, Badge, Typography, Progress

Layout:
- Hero section with module name and description
- Lecture grid (2 columns on desktop, 1 on mobile)
- Each lecture links to /module/[slug]/[lecture-slug] (route created later in Phase 6)
  </action>
  <verify>Navigate to /module/causal-analysis shows Module 1 with lectures</verify>
  <done>Module landing page displays module details and lecture list</done>
</task>

<task type="auto">
  <name>Task 2: Update layout to use real module data</name>
  <files>src/routes/+layout.svelte</files>
  <action>
Update root layout to:
1. Import modules from $lib/data/modules
2. Pass real module data to Sidebar component
3. Remove placeholder module array from 03-01

The Sidebar should now show all 8 modules with:
- Module name
- Lecture count
- Coming soon indicator for modules 2-8
- Active state based on current route (use $page.url from $app/stores)

Import { page } from '$app/stores' and use $page.url.pathname to determine active module.
  </action>
  <verify>Sidebar shows all 8 modules with correct data</verify>
  <done>Navigation uses real module data from centralized store</done>
</task>

<task type="auto">
  <name>Task 3: Update homepage with module overview</name>
  <files>src/routes/+page.svelte</files>
  <action>
Replace design system demo with app homepage:
- Hero section: "Data Analytics in Marketing" title, course description
- Module grid: Show all 8 modules as cards
- Each module card links to /module/[slug]
- Show lecture count and status per module
- Coming soon modules are visually distinct (muted colors, badge)

Use components from $lib/components/ui:
- Container, Grid, Card, Badge, Typography

Keep design consistent with design system patterns established in Phase 2.
Dark mode support.
  </action>
  <verify>Homepage shows all modules with navigation to each</verify>
  <done>Homepage serves as module overview and entry point</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete navigation system with module routes, sidebar, and homepage</what-built>
  <how-to-verify>
    1. Run: npm run dev -- --port 5180
    2. Visit: http://localhost:5180
    3. Verify homepage:
       - Shows all 8 modules
       - Module 1 (Causal Analysis) is clickable
       - Modules 2-8 show "Coming Soon"
    4. Click Module 1 card
    5. Verify module page:
       - Shows module name and description
       - Lists all 8 lectures
       - Lectures show status badges
    6. Test sidebar:
       - Desktop: Sidebar visible on left
       - Module 1 highlighted as active
       - Other modules listed
    7. Test mobile (resize to <768px):
       - Sidebar hidden
       - Hamburger menu in header
       - Menu opens sidebar overlay
    8. Test dark mode (toggle system preference)
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Homepage displays all modules
- [ ] /module/causal-analysis route works
- [ ] Navigation sidebar shows all modules
- [ ] Responsive layout works (desktop and mobile)
- [ ] Dark mode works throughout
- [ ] User approves navigation system
</verification>

<success_criteria>
- All tasks completed
- Navigation is fully functional
- Module routes work correctly
- Responsive design verified
- User approved checkpoint
- Ready for Phase 4: Visualization Engine
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-navigation/03-03-SUMMARY.md`
</output>
